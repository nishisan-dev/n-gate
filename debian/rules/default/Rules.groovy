/**
 * n-gate Default Rules
 *
 * This is a sample rule file that comes with the .deb package.
 * Customize it to define your routing logic.
 *
 * Available variables:
 *   - workload: the HttpWorkLoad object
 *   - context: the Javalin context
 *   - utils: utility map (gson, json, xml, httpClient)
 *   - upstreamRequest: the request adapter
 *   - listener: the listener name
 *   - contextName: the URL context name
 *   - requestMethod: the HTTP method
 */

// No custom processing — requests pass through to the default backend.
