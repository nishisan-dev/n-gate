# docker-compose.bench.yml
# Profile prod-like para benchmark: log level INFO, tracing desabilitado
#
# Uso:
#   docker compose -f docker-compose.yml -f docker-compose.bench.yml up -d
#
services:
  n-gate:
    environment:
      SPRING_PROFILES_DEFAULT: bench
      TRACING_ENABLED: "false"
    command:
      - /bin/bash
      - -lc
      - mvn -s /tmp/maven-settings.xml -DskipTests package && java -Dspring.profiles.active=bench -jar target/n-gate-1.0-SNAPSHOT.jar
